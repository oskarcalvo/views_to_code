<?php
// TODO finish the form to export the views code
// module_load_include('inc','views_to_code','views_to_code.routing');

/**
 * Implements hook_views_api().
 */
function views_to_code_views_api() {
  return array(
    'api' => 3,
    //'path' => drupal_get_path('module', 'views_to_code') . '/plugin',
    //'template path' => drupal_get_path('module', 'views_to_code') . '/themes',
  );
}
